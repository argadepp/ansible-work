version: 3
projects:
  - dir: resources/terragrunt
    workspace: default
    workflow: terraform
    autoplan:
      when_modified: ["*.tf", "*.tfvars","*.hcl"]
      enabled: true
workflows:
  terraform:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
