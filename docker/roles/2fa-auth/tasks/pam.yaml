- name: Configure PAM for OTP (safe rollout)
  template:
    src: sshd_pam.j2
    dest: /etc/pam.d/sshd
    owner: root
    group: root
    mode: '0644'
  when: pam_otp_enabled
  notify: restart sshd
